services:
  netpulse:
    build: .
    ports:
      - "4000:4000"
    env_file:
      - .env.docker
    environment:
      - NODE_ENV=production
      - PORT=4000
    volumes:
      - netpulse-data:/app/backend/data
    restart: unless-stopped

volumes:
  netpulse-data:
